// Convenience: putting public in the constructor like so will create the local variable without having to be declared
export class Ingredient {
  constructor(public name: string, public amount: number) {
  }
}
